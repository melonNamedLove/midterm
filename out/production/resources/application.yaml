spring:
  application:
    name: spring-test
  graphql:
    graphiql:
      enabled: true # graphiql? ?? ??? ?? ?? (localhost:8080/graphiql)
      printer:
        enabled: true # show-sql? ?? graphql ??? ?? ??